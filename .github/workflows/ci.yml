name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    name: Validate project structure & syntax
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1) 檔案結構檢查 — 確認所有 9+ 個關鍵檔案都存在且非空
      - name: File structure check
        run: |
          echo "=== File structure check ==="
          missing=0
          for f in LICENSE README.md \
                   index.html factors.html scoring.html backtest.html \
                   app.js data.js styles.css .gitignore .nojekyll \
                   backend/scrapers/base.py backend/scrapers/twse.py \
                   backend/scrapers/tpex.py backend/scrapers/finmind.py \
                   backend/scrapers/mops.py backend/scrapers/tdcc.py \
                   backend/scrapers/pyramid.py backend/scrapers/goodinfo.py \
                   backend/scrapers/us_markets.py \
                   backend/pipeline.py backend/normalize.py backend/schema.py \
                   backend/requirements.txt backend/.env.example \
                   backend/tests/test_rate_limiter.py \
                   backend/scripts/run_local.py backend/scripts/build_universe.py \
                   backend/scripts/auth_setup.py backend/scripts/windows_task.ps1 \
                   .github/workflows/scrape.yml .github/workflows/ci.yml \
                   Dockerfile docker-compose.yml; do
            if [ ! -s "$f" ]; then
              echo "::error::Missing or empty file: $f"
              missing=1
            else
              size=$(stat -c%s "$f")
              printf "  %-50s %8d bytes\n" "$f" "$size"
            fi
          done
          if [ $missing -ne 0 ]; then
            echo "::error::One or more required files are missing."
            exit 1
          fi
          echo "All required files present."

      # 1b) Data JSON shape validation（如果有資料檔）
      - name: Validate data/latest/*.json
        if: hashFiles('data/latest/meta.json') != ''
        run: |
          python -m pip install --quiet pydantic
          python -m backend.schema data/latest/prices.json
          python -m backend.schema data/latest/market_index.json
          python -m backend.schema data/latest/meta.json

      # 2) HTML well-formedness 檢查 — 用 Python html.parser 確認 4 個 HTML 檔案至少語法正確
      - name: HTML well-formedness check
        run: |
          echo "=== HTML well-formedness check ==="
          python3 - <<'PY'
          import sys
          from html.parser import HTMLParser

          class Checker(HTMLParser):
              def __init__(self):
                  super().__init__()
                  self.errors = []
              def error(self, msg):
                  self.errors.append(msg)

          files = ['index.html', 'factors.html', 'scoring.html', 'backtest.html']
          ok = True
          for f in files:
              try:
                  with open(f, encoding='utf-8') as fh:
                      content = fh.read()
                  c = Checker()
                  c.feed(content)
                  if c.errors:
                      print(f"  ::error::{f}: parse errors: {c.errors}")
                      ok = False
                  else:
                      print(f"  {f}: OK ({len(content)} bytes)")
              except Exception as e:
                  print(f"  ::error::{f}: {e}")
                  ok = False
          if not ok:
              sys.exit(1)
          PY

      # 3) JavaScript 語法檢查 — 用 Node.js `node --check` 對所有 JS 檔案
      - name: JavaScript syntax check
        run: |
          echo "=== JavaScript syntax check ==="
          for f in app.js data.js; do
            node --check "$f" && echo "  $f: syntax OK"
          done

      # 3b) Python unit tests (rate limiter + normalize)
      - name: Backend unit tests
        run: |
          echo "=== Backend unit tests ==="
          pip install --quiet pydantic
          python backend/tests/test_rate_limiter.py
          python backend/tests/test_normalize.py

      # 4) 連結檢查 — 用 lychee 確認 README 與 docs 內部連結有效
      - name: Link check (markdown & HTML)
        uses: lycheeverse/lychee-action@v2
        with:
          args: --no-progress --max-concurrency=4 --accept 200,206,403,429 --timeout 10 --exclude-loopback README.md
          fail: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}